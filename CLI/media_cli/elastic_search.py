#!/usr/bin/env python3
# coding: utf-8


def load_json(host, port, json_file):
    print(host, port, json_file)


def query_data(host, port, entity, key, value):
    print(host, port, entity, key, value)


def delete_entry(host, port, entity, id):
    print(host, port, entity, id)
